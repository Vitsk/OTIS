let initialState = {
  
}

const aparkReducer = (state= initialState, action) => {
  // switch (action.type) {
  //   case value:
  //     return {
  //       ...state
  //     }
  
  //   default:
  //     return state;
  // }
}

// AC


// Thunks

export default aparkReducer;